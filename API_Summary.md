# ☕️ Java 算法刷题核心 API 速查表 (Cheat Sheet)

> **Summary:** 汇总刷题中最高频使用的 Java API 与数据结构操作，拒绝提笔忘字。

---

## 1. 字符串 (String & StringBuilder)

**核心原则：** String 是不可变的，频繁修改拼接必须用 StringBuilder。

| 功能 | String 方法 | 备注 |
| :--- | :--- | :--- |
| **获取长度** | `s.length()` | **注意有括号！** |
| **获取字符** | `s.charAt(i)` | 返回 `char` |
| **转字符数组** | `s.toCharArray()` | **高频！** 方便遍历和修改 |
| **截取子串** | `s.substring(start, end)` | 左闭右开 `[start, end)` |
| **相等比较** | `s.equals("abc")` | **严禁使用 ==** |
| **忽略大小写比** | `s.equalsIgnoreCase("abc")` | |
| **去除首尾空格** | `s.trim()` | 不会去除中间空格 |
| **拆分数组** | `s.split("正则")` | 常用 `s.split("\\s+")` 切割多个空格 |
| **查找索引** | `s.indexOf("sub")` | 返回起始索引，找不到返 -1 |
| **包含** | `s.contains("sub")` | 返回 boolean |
| **替换** | `s.replace(old, new)` | 替换字符或字符串 |
| **数字转字符串** | `String.valueOf(123)` | 比 `""+123` 更高效 |

**StringBuilder (可变字符串)**

| 功能 | 方法 | 备注 |
| :--- | :--- | :--- |
| **追加** | `sb.append(e)` | 可以链式调用 |
| **插入** | `sb.insert(idx, e)` | 在指定位置插入 |
| **删除字符** | `sb.deleteCharAt(idx)` | **回溯算法常用** |
| **删除区间** | `sb.delete(start, end)` | 左闭右开 |
| **修改字符** | `sb.setCharAt(idx, char)` | |
| **反转** | `sb.reverse()` | 原地反转 |
| **转字符串** | `sb.toString()` | 最终输出时调用 |

---

## 2. 数组与列表 (Array & ArrayList)

**核心原则：** 知道长度用数组，不知道长度用 List。

| 操作 | 普通数组 (`int[]`) | 动态数组 (`ArrayList<E>`) |
| :--- | :--- | :--- |
| **获取长度** | `arr.length` (**无括号**) | `list.size()` (**有括号**) |
| **访问元素** | `arr[i]` | `list.get(i)` |
| **修改元素** | `arr[i] = 5;` | `list.set(i, 5);` |
| **尾部添加** | (不支持) | `list.add(e);` |
| **中间插入** | (不支持) | `list.add(idx, e);` |
| **删除元素** | (不支持) | `list.remove(idx);` |
| **判空** | `arr.length == 0` | `list.isEmpty()` |
| **包含** | (需遍历) | `list.contains(e);` |
| **排序** | `Arrays.sort(arr);` | `Collections.sort(list);` |
| **转列表** | `Arrays.asList(arr)` | - |
| **转数组** | - | `list.toArray(new Integer[0])` |

---

## 3. 栈与队列 (Deque 接口)

**核心原则：** 大厂规范统一使用 `Deque` (双端队列) 接口，实现类用 `LinkedList`。
> `Deque<Integer> dq = new LinkedList<>();`

| 角色 | 操作 | 方法名 (推荐) | 备注 |
| :--- | :--- | :--- | :--- |
| **栈 (Stack)** | **压栈** | `dq.push(e)` | 放在头部 |
| *(先进后出)* | **弹栈** | `dq.pop()` | 弹出头部，空栈抛异常 |
| | **看栈顶** | `dq.peek()` | 返回头部，空栈返 null |
| | | | |
| **队列 (Queue)** | **入队** | `dq.offer(e)` | 放在尾部 |
| *(先进先出)* | **出队** | `dq.poll()` | 弹出头部，空栈返 null |
| | **看队头** | `dq.peek()` | 返回头部 |
| | | | |
| **双端队列** | **头入/尾入** | `offerFirst` / `offerLast` | |
| | **头出/尾出** | `pollFirst` / `pollLast` | |
| | **看头/看尾** | `peekFirst` / `peekLast` | |

---

## 4. 哈希表 (HashMap & HashSet)

**核心原则：** O(1) 时间复杂度的查找神器。

| 功能 | HashMap (`map`) | HashSet (`set`) |
| :--- | :--- | :--- |
| **增/改** | `map.put(key, val)` | `set.add(key)` |
| **查值** | `map.get(key)` | - |
| **查值(防空)** | `map.getOrDefault(k, def)` | **统计计数神器** |
| **查存在** | `map.containsKey(key)` | `set.contains(key)` |
| **删除** | `map.remove(key)` | `set.remove(key)` |
| **大小** | `map.size()` | `set.size()` |
| **判空** | `map.isEmpty()` | `set.isEmpty()` |
| **清空** | `map.clear()` | `set.clear()` |

**遍历 Map 的两种方式：**
1. `for (K key : map.keySet()) { ... }` (只用 Key)
2. `for (Map.Entry<K,V> entry : map.entrySet()) { entry.getKey(); ... }` (效率高)

---

## 5. 数字与数学 (Math & Wrapper)

| 功能 | 方法 | 备注 |
| :--- | :--- | :--- |
| **最大/最小值** | `Math.max(a, b)`, `Math.min(a, b)` | |
| **绝对值** | `Math.abs(a)` | |
| **幂运算** | `Math.pow(a, b)` | 返回 double |
| **平方根** | `Math.sqrt(a)` | 返回 double |
| **四舍五入** | `Math.round(a)` | 返回 long/int |
| **整型最大值** | `Integer.MAX_VALUE` | `2^31 - 1` |
| **整型最小值** | `Integer.MIN_VALUE` | `-2^31` |
| **字符转数字** | `Character.getNumericValue(c)` | 或 `c - '0'` |
| **字符串转Int** | `Integer.parseInt(s)` | |

---

## 6. 字符工具 (Character)

| 功能 | 方法 |
| :--- | :--- |
| **判断数字** | `Character.isDigit(c)` |
| **判断字母** | `Character.isLetter(c)` |
| **判断小写** | `Character.isLowerCase(c)` |
| **判断大写** | `Character.isUpperCase(c)` |
| **转小写** | `Character.toLowerCase(c)` |
| **转大写** | `Character.toUpperCase(c)` |

---

## 🧠 记忆口诀

1.  **长度谁没括号？**
    * 只有数组 `[]` 是亲生的，属性 `.length`。
    * 其他全是领养的，方法 `.length()` 或 `.size()`。
2.  **栈和队列怎么分？**
    * 栈 (`push/pop`)：像压子弹，很暴力。
    * 队列 (`offer/poll`)：像给供品 (`offer`) 和投票 (`poll`)，很文明。
3.  **Map 取值怕空？**
    * 永远优先用 `getOrDefault`，少写一行 `if` 判空。